# Server — Flask + WebSocket
flask>=2.0
flask-sock>=0.6

# Production server with WebSocket support (eventlet works behind Cloud Run proxy; gevent-websocket can trigger 1002)
gunicorn>=21.0
eventlet>=0.35.0
